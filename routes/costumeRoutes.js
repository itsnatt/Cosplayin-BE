// costumeRoutes.js
const express = require('express');
const router = express.Router();

// Route untuk mendapatkan daftar kostum
router.get('/', (req, res) => {
  // Logika untuk mendapatkan daftar kostum dari database
});

module.exports = router;
